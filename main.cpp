#include <iostream>
#include <string>
#include <vector>

#include "ExperimentController.h"

using namespace std;

int main() {
  // ExperimentController::runExperiment("three.txt", 1);
  // ExperimentController::runExperiment("three.txt", 2);
  ExperimentController::runExperiment("solvable_twos.txt", 2);

  return 0;
}