#ifndef SOLVER
#define SOLVER

#include <iostream>
#include <vector>

using namespace std;

// Solver is a parent class

/*
class Solver {
  // a lot of these might have to change depending on the kind of data we need for performance measure
 public:
  void solve();

 protected:
  vector<vector<int>> board;
  void makeMove();
  // virtual int calculateHeuristic();
};
*/

#endif